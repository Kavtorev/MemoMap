<Page
    x:Class="MemoMap.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MemoMap.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"    
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded"
    RequestedTheme="{x:Bind MainViewModel.Theme, Mode=OneWay}">

    <!-- Navigation menu -->
   <Grid>
        <NavigationView IsSettingsVisible="True" ItemInvoked="NavigationView_ItemInvoked">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="View groups" Icon="List" Tag="view_groups"/>
                <NavigationViewItem Content="My Maps" Icon="Map" Tag="my_maps"/>
                <NavigationViewItem Content="Create a group" Icon="Edit" Tag="create_group" />
                <NavigationViewItem Content="Map" Icon="Map" Tag="map"/>
                <NavigationViewItem Content="Account settings" Icon="Setting" Tag="account_settings"/>
            </NavigationView.MenuItems>
            <Grid Padding="10, 0, 10, 10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{x:Bind MainViewModel.PageTitle, Mode=OneWay}" />
                    <Button Background="Transparent" x:Name="ThemeChanger" Click="ThemeChanger_Click" HorizontalAlignment="Right">
                        <Image x:Name="ThemeIcon" Source="{x:Bind MainViewModel.SourceIconImage, Mode=OneWay}" Height="30" />
                    </Button>
                </StackPanel>
                <Frame x:Name="MainFrame"/>
            </Grid>
        </NavigationView>
    </Grid>

</Page> 
